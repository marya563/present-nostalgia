#include <stdio.h>
#include <stdlib.h>
#include "SDL/SDL.h"
#include "SDL/SDL_image.h"
#include "SDL/SDL_ttf.h"

void fin_jeu()
{
if ((v.val==-1) && (t.min>01))
{

printf("perdu");
//animation_fail(p);
//image perdant ;




}
else if ((t.min<=01) && (nb_potion==10))
{
printf("gagnant");
// musique 
}
}
